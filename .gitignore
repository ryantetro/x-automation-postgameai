.env
.env.local
.env.*.local

/node_modules
node_modules/
/.vercel

# Local/generated (avoid noisy local artifacts in status)
.whisper-models/
Recording*

# Canopy sample and audit artifacts
/state/canopy/sample-*.png
/state/canopy/sample-tweets-*.json
/state/canopy/sample-tweets-*.md
/state/canopy/launch-audit-*.json
/state/canopy/launch-audit-*.md

# Codex local configuration
.codex/

# RuFlo / Claude Code — local only (re-run `npx ruflo@latest init` on a fresh clone)
/.claude/
/.agents/
/.mcp.json
/CLAUDE.md
/AGENTS.md
**/.claude-flow/
